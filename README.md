# scp
SFTP Server dengan 
Username == IP Address 
Password == nomor presensi
Port == 50123

Windows compile for stealth windows
```sh
go build -ldflags -H=windowsgui
```

```sh
git tag v0.1.2
git push origin --tags
```